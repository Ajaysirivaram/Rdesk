{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "cd fullstack/backend && python manage.py migrate && python manage.py create_sample_departments && gunicorn camelq_payslip.wsgi --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/api/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
